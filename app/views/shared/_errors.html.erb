<% if record_object.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(record_object.errors.count, "error") %> prohibited this post from being saved:</h2>

    <ul>
      <% record_object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>